using System.IO;

namespace Zipper.Core.Utils
{
    public static class FileTypeChecker
    {
        public static bool IsFileBinary(Stream content)
        {
            return false;
        }
    }
}